#pragma once

/*
 * turn off mongoose authentication
 */
#define MG_DISABLE_HTTP_DIGEST_AUTH 1
#define MG_DISABLE_SHA1 1
#define MG_DISABLE_MD5 1

/* 
 * Disable loguru's unsafe signal handlers
 */
#define LOGURU_UNSAFE_SIGNAL_HANDLER 0

/*
 * These values are automatically set according to their cmake variables.
 */

#define HAVE_QT5CORE ${Qt5Core_FOUND}

#define HAVE_QT5WIDGETS ${Qt5Widgets_FOUND}

